'use strict';

const test = require('ava');

const config = {
  logging: function () {}
};
let db = null;
test.beforeEach(async () => {
  const setupDatabase = require('../');

  db = await setupDatabase(config);
});
test('Agent', t => {
  t.truthy(db.Agent, 'Agent service should exist');
});
//# sourceMappingURL=/home/dipzo/DIPZO/node/avanzado/Platzi/Platziverse/platziverse-db/node_modules/.cache/ava/4de6eefcfb5704506faa8b4ba2acce58.js.map